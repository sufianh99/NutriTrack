---
phase: 03-quality-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/__init__.py
  - app/routes.py
  - app/logging_config.py
autonomous: true
requirements:
  - QUAL-06
  - QUAL-07

must_haves:
  truths:
    - "GET /health returns JSON {\"status\": \"ok\"} with HTTP 200"
    - "Application startup writes a log line via Python logging module"
    - "Profile save events are written to application logs"
    - "Food entry add/edit/delete events are written to application logs"
  artifacts:
    - path: "app/logging_config.py"
      provides: "Centralized logging setup"
      contains: "logging.getLogger"
    - path: "app/routes.py"
      provides: "Health endpoint and log calls in route handlers"
      contains: "def health"
  key_links:
    - from: "app/__init__.py"
      to: "app/logging_config.py"
      via: "import and call in create_app"
      pattern: "configure_logging|logging_config"
    - from: "app/routes.py"
      to: "logging"
      via: "logger.info calls in route handlers"
      pattern: "logger\\.info"
---

<objective>
Add structured Python logging across the application and a /health endpoint.

Purpose: QUAL-06 requires Python logging integration for app startup, profile saves, and food entry events. QUAL-07 requires a /health endpoint returning {"status": "ok"}.
Output: app/logging_config.py, updated app/__init__.py and app/routes.py with logging and health route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/__init__.py
@app/routes.py
@config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create logging configuration and integrate into app factory</name>
  <files>app/logging_config.py, app/__init__.py</files>
  <read_first>app/__init__.py, config.py</read_first>
  <action>
Create `app/logging_config.py` with a `configure_logging(app)` function that:
- Gets a logger named "nutritrack" via `logging.getLogger("nutritrack")`
- Sets level to `logging.INFO`
- Adds a `logging.StreamHandler()` to the logger
- Uses format string: `"%(asctime)s [%(levelname)s] %(name)s: %(message)s"`
- Adds the handler only if the logger has no handlers yet (prevents duplicate handlers during testing)
- Logs `logger.info("NutriTrack application started")` at the end of configuration

Update `app/__init__.py` to:
- Add `from app.logging_config import configure_logging` import
- Call `configure_logging(app)` inside `create_app()` AFTER `db.init_app(app)` but BEFORE blueprint registration
- The function signature and return type of `create_app` must remain unchanged

Type annotations required on `configure_logging`:
```python
import logging
from flask import Flask

def configure_logging(app: Flask) -> None:
```
  </action>
  <verify>
    <automated>cd C:/Repos/NutriTrack && python -c "from app import create_app; app = create_app(); print('OK')" 2>&1 | grep -E "NutriTrack application started|OK"</automated>
  </verify>
  <acceptance_criteria>
    - app/logging_config.py contains `def configure_logging(app: Flask) -> None:`
    - app/logging_config.py contains `logging.getLogger("nutritrack")`
    - app/logging_config.py contains `logger.info("NutriTrack application started")`
    - app/__init__.py contains `from app.logging_config import configure_logging`
    - app/__init__.py contains `configure_logging(app)`
  </acceptance_criteria>
  <done>Logging module exists, app startup produces a log line, app still boots cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Add /health endpoint and logging to route handlers</name>
  <files>app/routes.py</files>
  <read_first>app/routes.py, app/logging_config.py</read_first>
  <action>
Update `app/routes.py` to:

1. Add imports at top:
   ```python
   import logging
   from flask import jsonify
   ```
   (jsonify may already be importable from flask; add it to the existing flask import line)

2. Create module-level logger:
   ```python
   logger = logging.getLogger("nutritrack")
   ```

3. Add health endpoint BEFORE the index route:
   ```python
   @bp.route("/health")
   def health():
       """Health check endpoint for monitoring and CI verification."""
       return jsonify({"status": "ok"}), 200
   ```

4. Add logging calls to existing routes (do NOT change route logic, only ADD logger lines):
   - In `_save_profile_and_goals`, after `db.session.commit()`: `logger.info("Profile saved: age=%d, weight=%.1f, height=%.1f, goal=%s", form.age.data, form.weight_kg.data, form.height_cm.data, form.goal.data)`
   - In `add_food`, after `db.session.commit()`: `logger.info("Food entry added: %s, %.1fg", form.name.data, form.amount_g.data)`
   - In `edit_food`, after `db.session.commit()`: `logger.info("Food entry updated: id=%d, %s", entry_id, form.name.data)`
   - In `delete_food`, after `db.session.delete(entry)` and `db.session.commit()`: `logger.info("Food entry deleted: id=%d", entry_id)`

All route function signatures, return values, and existing logic must remain UNCHANGED. Only add `logger.info(...)` lines and the new `/health` route.
  </action>
  <verify>
    <automated>cd C:/Repos/NutriTrack && python -c "from app import create_app; app = create_app(); client = app.test_client(); r = client.get('/health'); print(r.status_code, r.get_json())"</automated>
  </verify>
  <acceptance_criteria>
    - app/routes.py contains `def health():`
    - app/routes.py contains `return jsonify({"status": "ok"}), 200`
    - app/routes.py contains `logger = logging.getLogger("nutritrack")`
    - app/routes.py contains `logger.info("Profile saved:`
    - app/routes.py contains `logger.info("Food entry added:`
    - app/routes.py contains `logger.info("Food entry updated:`
    - app/routes.py contains `logger.info("Food entry deleted:`
    - GET /health returns HTTP 200 with body {"status": "ok"}
  </acceptance_criteria>
  <done>/health returns {"status": "ok"} with 200, all CRUD routes log their events via Python logging module</done>
</task>

</tasks>

<verification>
- `python -c "from app import create_app; app = create_app(); client = app.test_client(); r = client.get('/health'); assert r.status_code == 200; assert r.get_json() == {'status': 'ok'}; print('PASS')"` prints PASS
- `grep -c "logger.info" app/routes.py` returns 4 or more
- `grep "configure_logging" app/__init__.py` finds the call
</verification>

<success_criteria>
- /health endpoint returns {"status": "ok"} with HTTP 200
- Python logging module used via getLogger("nutritrack")
- Startup, profile save, food add/edit/delete all produce log lines
- Existing tests still pass (no route behavior changed)
</success_criteria>

<output>
After completion, create `.planning/phases/03-quality-gates/03-01-SUMMARY.md`
</output>
