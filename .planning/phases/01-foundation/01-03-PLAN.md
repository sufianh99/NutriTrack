---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - app/routes.py
  - app/templates/onboarding.html
  - app/templates/profile.html
  - app/templates/dashboard.html
autonomous: false
requirements:
  - PROF-01
  - PROF-06

must_haves:
  truths:
    - "User is redirected to /onboarding when no profile exists (fresh DB)"
    - "User can submit onboarding form with age, height, weight, gender, activity level, goal"
    - "After onboarding, BMR/TDEE/calorie goal/macros are calculated and stored in DailyGoal"
    - "After onboarding, user is redirected to dashboard which shows Soll values"
    - "User can re-edit profile at /profile and DailyGoal is recalculated"
    - "Dashboard shows calorie goal and macro targets (protein, fat, carbs in grams)"
  artifacts:
    - path: "app/routes.py"
      provides: "Full routes: index, onboarding (GET+POST), profile (GET+POST), dashboard"
      contains: "def onboarding"
      min_lines: 60
    - path: "app/templates/onboarding.html"
      provides: "Onboarding form template with German labels"
      contains: "form.age"
    - path: "app/templates/profile.html"
      provides: "Profile edit form, pre-filled"
      contains: "form.age"
    - path: "app/templates/dashboard.html"
      provides: "Dashboard with Soll values display"
      contains: "calorie_goal"
  key_links:
    - from: "app/routes.py"
      to: "app/calculator.py"
      via: "import and call chain"
      pattern: "from app.calculator import"
    - from: "app/routes.py"
      to: "app/models.py"
      via: "UserProfile and DailyGoal CRUD"
      pattern: "from app.models import UserProfile, DailyGoal"
    - from: "app/routes.py"
      to: "app/forms.py"
      via: "OnboardingForm instantiation"
      pattern: "from app.forms import OnboardingForm"
    - from: "app/templates/dashboard.html"
      to: "app/routes.py"
      via: "template context variables"
      pattern: "goal.calorie_goal"
---

<objective>
Wire the onboarding flow, profile editing, and dashboard display. After this plan, a user can complete onboarding, see their calculated goals, and edit their profile.

Purpose: PROF-01 (onboarding form) and PROF-06 (view calculated goals) are the user-facing deliverables of Phase 1.
Output: Working onboarding-to-dashboard flow with calculator integration
</objective>

<execution_context>
@C:\Users\konst\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\konst\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01: app/models.py -->
From app/models.py:
```python
class UserProfile(db.Model):
    __tablename__ = "user_profile"
    id: Mapped[int]       # primary key, always 1
    age: Mapped[int]
    height_cm: Mapped[float]
    weight_kg: Mapped[float]
    gender: Mapped[str]         # "male" | "female"
    activity_level: Mapped[str] # "sedentary"|"light"|"moderate"|"active"|"very_active"
    goal: Mapped[str]           # "lose"|"maintain"|"gain"

class DailyGoal(db.Model):
    __tablename__ = "daily_goal"
    id: Mapped[int]
    date: Mapped[date]          # unique
    calorie_goal: Mapped[float]
    protein_goal: Mapped[float]
    fat_goal: Mapped[float]
    carb_goal: Mapped[float]
```

<!-- From Plan 01: app/forms.py -->
From app/forms.py:
```python
class OnboardingForm(FlaskForm):
    age = IntegerField(...)
    height_cm = FloatField(...)
    weight_kg = FloatField(...)
    gender = SelectField(...)
    activity_level = SelectField(...)
    goal = SelectField(...)
    submit = SubmitField("Speichern")
```

<!-- From Plan 02: app/calculator.py -->
From app/calculator.py:
```python
def calculate_bmr(weight_kg: float, height_cm: float, age: int, gender: str) -> float
def calculate_tdee(bmr: float, activity_level: str) -> float
def apply_goal_modifier(tdee: float, goal: str) -> float
def calculate_macros(calorie_goal: float) -> dict[str, float]
    # Returns {"protein_g": float, "fat_g": float, "carbs_g": float}
```

<!-- From Plan 01: app/__init__.py -->
From app/__init__.py:
```python
db = SQLAlchemy()
def create_app(config_class=Config): ...
```

<!-- From Plan 01: app/routes.py stub structure -->
From app/routes.py:
```python
bp = Blueprint("main", __name__)
def _get_profile(): return db.session.get(UserProfile, 1)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement full routes and templates</name>
  <read_first>
    - app/routes.py (stub from Plan 01 — to be expanded)
    - app/calculator.py (function signatures and imports)
    - app/models.py (UserProfile and DailyGoal field names)
    - app/forms.py (OnboardingForm field names)
    - app/templates/base.html (layout shell to extend)
    - app/templates/onboarding.html (stub from Plan 01 — to be replaced)
    - app/templates/dashboard.html (stub from Plan 01 — to be replaced)
  </read_first>
  <files>app/routes.py, app/templates/onboarding.html, app/templates/profile.html, app/templates/dashboard.html</files>
  <action>
    **Step 1: Rewrite app/routes.py with full route handlers**

    Replace the Plan 01 stub with complete implementation:

    ```python
    from datetime import date

    from flask import Blueprint, flash, redirect, render_template, url_for

    from app import db
    from app.calculator import (
        apply_goal_modifier,
        calculate_bmr,
        calculate_macros,
        calculate_tdee,
    )
    from app.forms import OnboardingForm
    from app.models import DailyGoal, UserProfile

    bp = Blueprint("main", __name__)


    def _get_profile() -> UserProfile | None:
        """Return the single-user profile (id=1) or None."""
        return db.session.get(UserProfile, 1)


    def _save_profile_and_goals(form: OnboardingForm) -> None:
        """Create/update UserProfile and upsert today's DailyGoal."""
        profile = db.session.get(UserProfile, 1) or UserProfile(id=1)
        profile.age = form.age.data
        profile.height_cm = form.height_cm.data
        profile.weight_kg = form.weight_kg.data
        profile.gender = form.gender.data
        profile.activity_level = form.activity_level.data
        profile.goal = form.goal.data
        db.session.merge(profile)

        bmr = calculate_bmr(
            form.weight_kg.data, form.height_cm.data, form.age.data, form.gender.data
        )
        tdee = calculate_tdee(bmr, form.activity_level.data)
        calorie_goal = apply_goal_modifier(tdee, form.goal.data)
        macros = calculate_macros(calorie_goal)

        today = date.today()
        goal_row = DailyGoal.query.filter_by(date=today).first()
        if goal_row is None:
            goal_row = DailyGoal(date=today)
        goal_row.calorie_goal = round(calorie_goal, 2)
        goal_row.protein_goal = macros["protein_g"]
        goal_row.fat_goal = macros["fat_g"]
        goal_row.carb_goal = macros["carbs_g"]
        db.session.merge(goal_row)
        db.session.commit()


    @bp.route("/")
    def index():
        profile = _get_profile()
        if profile is None:
            return redirect(url_for("main.onboarding"))
        return redirect(url_for("main.dashboard"))


    @bp.route("/onboarding", methods=["GET", "POST"])
    def onboarding():
        form = OnboardingForm()
        if form.validate_on_submit():
            _save_profile_and_goals(form)
            flash("Profil gespeichert! Deine Ziele wurden berechnet.", "success")
            return redirect(url_for("main.dashboard"))
        return render_template("onboarding.html", form=form)


    @bp.route("/profile", methods=["GET", "POST"])
    def profile():
        profile = _get_profile()
        if profile is None:
            return redirect(url_for("main.onboarding"))
        form = OnboardingForm(obj=profile)
        if form.validate_on_submit():
            _save_profile_and_goals(form)
            flash("Profil aktualisiert! Ziele neu berechnet.", "success")
            return redirect(url_for("main.dashboard"))
        return render_template("profile.html", form=form, profile=profile)


    @bp.route("/dashboard")
    def dashboard():
        profile = _get_profile()
        if profile is None:
            return redirect(url_for("main.onboarding"))
        today = date.today()
        goal = DailyGoal.query.filter_by(date=today).first()
        return render_template("dashboard.html", profile=profile, goal=goal)
    ```

    Key implementation details:
    - `_save_profile_and_goals()` shared between onboarding and profile (per D-04, D-05)
    - Profile route uses `OnboardingForm(obj=profile)` to pre-fill fields (per D-04)
    - Dashboard passes `profile` and `goal` context — no `current_user` anywhere
    - Every route except `/onboarding` guards with `_get_profile()` and redirects if None (per D-01)
    - Flash messages in German (per D-08)

    **Step 2: Create app/templates/onboarding.html**

    Full form template with all 6 fields, German labels, Bootstrap 5 styling:

    ```html
    {% extends "base.html" %}
    {% block title %}Onboarding{% endblock %}
    {% block content %}
    <div class="row justify-content-center">
      <div class="col-md-8 col-lg-6">
        <h2 class="mb-4">Willkommen bei NutriTrack</h2>
        <p class="text-muted mb-4">Gib deine Daten ein, um deinen persoenlichen Tagesbedarf zu berechnen.</p>
        <form method="POST" novalidate>
          {{ form.hidden_tag() }}

          <div class="mb-3">
            {{ form.age.label(class="form-label") }}
            {{ form.age(class="form-control", placeholder="z.B. 30") }}
            {% for error in form.age.errors %}
            <div class="text-danger small">{{ error }}</div>
            {% endfor %}
          </div>

          <div class="mb-3">
            {{ form.height_cm.label(class="form-label") }}
            {{ form.height_cm(class="form-control", placeholder="z.B. 175") }}
            {% for error in form.height_cm.errors %}
            <div class="text-danger small">{{ error }}</div>
            {% endfor %}
          </div>

          <div class="mb-3">
            {{ form.weight_kg.label(class="form-label") }}
            {{ form.weight_kg(class="form-control", placeholder="z.B. 70") }}
            {% for error in form.weight_kg.errors %}
            <div class="text-danger small">{{ error }}</div>
            {% endfor %}
          </div>

          <div class="mb-3">
            {{ form.gender.label(class="form-label") }}
            {{ form.gender(class="form-select") }}
            {% for error in form.gender.errors %}
            <div class="text-danger small">{{ error }}</div>
            {% endfor %}
          </div>

          <div class="mb-3">
            {{ form.activity_level.label(class="form-label") }}
            {{ form.activity_level(class="form-select") }}
            {% for error in form.activity_level.errors %}
            <div class="text-danger small">{{ error }}</div>
            {% endfor %}
          </div>

          <div class="mb-3">
            {{ form.goal.label(class="form-label") }}
            {{ form.goal(class="form-select") }}
            {% for error in form.goal.errors %}
            <div class="text-danger small">{{ error }}</div>
            {% endfor %}
          </div>

          {{ form.submit(class="btn btn-success btn-lg w-100") }}
        </form>
      </div>
    </div>
    {% endblock %}
    ```

    **Step 3: Create app/templates/profile.html**

    Same form as onboarding but with heading "Profil bearbeiten" and a link back to dashboard:

    ```html
    {% extends "base.html" %}
    {% block title %}Profil{% endblock %}
    {% block content %}
    <div class="row justify-content-center">
      <div class="col-md-8 col-lg-6">
        <h2 class="mb-4">Profil bearbeiten</h2>
        <form method="POST" novalidate>
          {{ form.hidden_tag() }}
          <!-- Same field blocks as onboarding.html -->
          <!-- All 6 fields: age, height_cm, weight_kg, gender, activity_level, goal -->
          <!-- Each with form-label, form-control/form-select, error display -->
          {{ form.submit(class="btn btn-success btn-lg w-100") }}
        </form>
        <a href="{{ url_for('main.dashboard') }}" class="btn btn-outline-secondary mt-3 w-100">Zurueck zum Dashboard</a>
      </div>
    </div>
    {% endblock %}
    ```
    Include all 6 field blocks identical to onboarding.html.

    **Step 4: Rewrite app/templates/dashboard.html** (per D-06, D-07)

    Show calculated Soll values (calorie goal + macro targets). No tracking data yet (Phase 2).

    ```html
    {% extends "base.html" %}
    {% block title %}Dashboard{% endblock %}
    {% block content %}
    <h2 class="mb-4">Dashboard</h2>

    {% if goal %}
    <div class="row mt-3">
      <div class="col-md-3 mb-3">
        <div class="card text-bg-success">
          <div class="card-body text-center">
            <h5 class="card-title">Kalorienziel</h5>
            <p class="display-6">{{ "%.0f"|format(goal.calorie_goal) }} kcal</p>
          </div>
        </div>
      </div>
      <div class="col-md-3 mb-3">
        <div class="card border-success">
          <div class="card-body text-center">
            <h5 class="card-title">Protein</h5>
            <p class="display-6">{{ "%.1f"|format(goal.protein_goal) }} g</p>
          </div>
        </div>
      </div>
      <div class="col-md-3 mb-3">
        <div class="card border-success">
          <div class="card-body text-center">
            <h5 class="card-title">Fett</h5>
            <p class="display-6">{{ "%.1f"|format(goal.fat_goal) }} g</p>
          </div>
        </div>
      </div>
      <div class="col-md-3 mb-3">
        <div class="card border-success">
          <div class="card-body text-center">
            <h5 class="card-title">Kohlenhydrate</h5>
            <p class="display-6">{{ "%.1f"|format(goal.carb_goal) }} g</p>
          </div>
        </div>
      </div>
    </div>
    {% else %}
    <div class="alert alert-info">
      Keine Ziele fuer heute berechnet. <a href="{{ url_for('main.profile') }}">Profil aktualisieren</a>.
    </div>
    {% endif %}

    <a href="{{ url_for('main.profile') }}" class="btn btn-outline-success mt-3">Profil bearbeiten</a>
    {% endblock %}
    ```

    Dashboard shows 4 cards: calorie goal, protein, fat, carbs. German labels per D-08.
    No tracking/Ist section yet (that's Phase 2 DASH-01/02/03).

    **Step 5: Update base.html navbar**
    Ensure the nav links point to correct routes. The "Profil" link should go to `main.profile` (not `main.onboarding`):
    ```html
    <li class="nav-item"><a class="nav-link" href="{{ url_for('main.dashboard') }}">Dashboard</a></li>
    <li class="nav-item"><a class="nav-link" href="{{ url_for('main.profile') }}">Profil</a></li>
    ```
  </action>
  <verify>
    <automated>cd C:/Repos/NutriTrack && python -c "
from app import create_app, db
from config import TestConfig
app = create_app(TestConfig)
client = app.test_client()
with app.app_context():
    db.create_all()
    # Test 1: Root redirects to onboarding when no profile
    r = client.get('/', follow_redirects=False)
    assert r.status_code == 302 and '/onboarding' in r.headers['Location'], f'Root redirect failed: {r.status_code}'
    # Test 2: Onboarding GET returns 200
    r = client.get('/onboarding')
    assert r.status_code == 200, f'Onboarding GET failed: {r.status_code}'
    # Test 3: Onboarding POST creates profile and redirects to dashboard
    r = client.post('/onboarding', data={'age':'30','height_cm':'175','weight_kg':'70','gender':'male','activity_level':'sedentary','goal':'maintain'}, follow_redirects=False)
    assert r.status_code == 302 and '/dashboard' in r.headers['Location'], f'Onboarding POST failed: {r.status_code}'
    # Test 4: Dashboard shows calorie goal after onboarding
    r = client.get('/dashboard')
    assert r.status_code == 200 and b'kcal' in r.data, f'Dashboard failed: {r.status_code}'
    # Test 5: Profile GET pre-fills data
    r = client.get('/profile')
    assert r.status_code == 200, f'Profile GET failed: {r.status_code}'
    print('ALL ROUTE TESTS PASSED')
"</automated>
  </verify>
  <acceptance_criteria>
    - app/routes.py contains "def onboarding():" with both GET and POST methods
    - app/routes.py contains "def profile():" with both GET and POST methods
    - app/routes.py contains "def dashboard():" that passes "profile" and "goal" to template
    - app/routes.py contains "from app.calculator import" (wiring to calculator module)
    - app/routes.py contains "from app.models import DailyGoal, UserProfile"
    - app/routes.py contains "from app.forms import OnboardingForm"
    - app/routes.py contains "_save_profile_and_goals" helper function
    - app/routes.py contains 'flash("Profil gespeichert' (German flash message)
    - app/templates/onboarding.html contains "form.age" and "form.height_cm" and "form.weight_kg" and "form.gender" and "form.activity_level" and "form.goal"
    - app/templates/onboarding.html contains "form.hidden_tag()" (CSRF token)
    - app/templates/onboarding.html contains "Willkommen bei NutriTrack"
    - app/templates/profile.html contains "Profil bearbeiten" and "form.age"
    - app/templates/profile.html contains link to "main.dashboard"
    - app/templates/dashboard.html contains "goal.calorie_goal" and "goal.protein_goal" and "goal.fat_goal" and "goal.carb_goal"
    - app/templates/dashboard.html contains "Kalorienziel" and "Protein" and "Fett" and "Kohlenhydrate"
    - app/templates/dashboard.html does NOT contain "current_user"
    - GET / with no profile returns 302 redirect to /onboarding
    - POST /onboarding with valid data returns 302 redirect to /dashboard
    - GET /dashboard after onboarding returns 200 with "kcal" in body
    - GET /profile after onboarding returns 200
  </acceptance_criteria>
  <done>User can complete onboarding, see calculated calorie and macro goals on dashboard, and re-edit profile. Full flow: onboarding form -> calculator chain -> DailyGoal upsert -> dashboard display.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify onboarding-to-dashboard flow</name>
  <files>None (visual verification only)</files>
  <action>
    Human visually verifies the complete Phase 1 flow by running the app locally and testing the onboarding-to-dashboard pipeline. No code changes needed.
  </action>
  <what-built>Complete Phase 1 onboarding flow: form submission, BMR/TDEE/macro calculation, dashboard goal display, profile editing</what-built>
  <how-to-verify>
    1. Delete instance/nutritrack.db if it exists
    2. Run: `cd C:/Repos/NutriTrack && python run.py`
    3. Open http://127.0.0.1:5000/ in browser — should redirect to /onboarding
    4. Fill in: Age=30, Height=175, Weight=70, Gender=Maennlich, Activity=Sitzend, Goal=Halten
    5. Click "Speichern" — should redirect to dashboard
    6. Verify dashboard shows: ~1979 kcal goal (1648.75 BMR * 1.2 TDEE * 1.0 maintain = 1978.50), protein ~123.7g, fat ~66.0g, carbs ~222.8g
    7. Click "Profil bearbeiten" — should show pre-filled form
    8. Change Goal to "Abnehmen", save — dashboard calorie goal should drop to ~1682 kcal (1978.50 * 0.85)
    9. All text should be in German
  </how-to-verify>
  <verify>Human confirms all 9 steps pass</verify>
  <done>Human has verified the onboarding-to-dashboard flow works correctly with proper calculations and German UI</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Root URL redirects to /onboarding on fresh DB
2. Onboarding form accepts all 6 fields and saves profile + daily goal
3. Dashboard displays calorie goal and 3 macro targets
4. Profile edit recalculates goals
5. All UI text is in German
6. No current_user references anywhere
</verification>

<success_criteria>
- User can complete onboarding and see calculated goals on dashboard
- Profile editing recalculates and updates DailyGoal
- Dashboard shows calorie goal, protein, fat, carbs (Soll values)
- All text in German
- Visual verification by human confirms the flow works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
